import { useMemo, useState } from 'react'
import { ChevronLeft, ChevronRight } from 'lucide-react'
import { useMarketingStore } from '../../../lib/marketingStore'
export function ContentCalendarPage(){const contents=useMarketingStore(s=>s.contents);const[current,setCurrent]=useState(new Date());const days=useMemo(()=>{const y=current.getFullYear(),m=current.getMonth(),first=new Date(y,m,1),start=new Date(y,m,1-first.getDay());return Array.from({length:42},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d})},[current]);return <div className="page"><header className="page-header"><div><span className="eyebrow">CALENDÁRIO EDITORIAL</span><h1>{current.toLocaleDateString('pt-BR',{month:'long',year:'numeric'})}</h1><p>Conteúdos planejados por data.</p></div><div className="calendar-controls"><button type="button" onClick={()=>setCurrent(new Date(current.getFullYear(),current.getMonth()-1,1))}><ChevronLeft/></button><button type="button" onClick={()=>setCurrent(new Date())}>Hoje</button><button type="button" onClick={()=>setCurrent(new Date(current.getFullYear(),current.getMonth()+1,1))}><ChevronRight/></button></div></header><section className="content-calendar panel-card">{['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'].map(x=><div key={x} className="calendar-weekday">{x}</div>)}{days.map(day=>{const items=contents.filter(i=>i.scheduledAt&&new Date(i.scheduledAt).toDateString()===day.toDateString());return <div key={day.toISOString()} className={`calendar-cell ${day.getMonth()!==current.getMonth()?'outside':''}`}><strong>{day.getDate()}</strong><div className="calendar-items">{items.map(i=><div key={i.id} className={`calendar-content ${i.network}`}>{i.title}</div>)}</div></div>})}</section></div>}
